{
  "name": "thepresentistemporalpurgatory",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "git lfs pull && vite build",
    "preview": "vite preview",
    "vercel-build": "curl -s https://packagecloud.io/install/repositories/github/git-lfs/script.deb.sh | sudo bash && sudo apt-get install git-lfs && git lfs pull && vite build"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  },
  "dependencies": {
    "three": "^0.171.0",
    "vercel": "^39.2.5",
    "vite": "^6.0.7"
  }
}
